import gdb
import time

# class Split(gdb.Command):
#     """Split the layout in GDB"""
#
#     def __init__(self):
#         super(Split, self).__init__("split", gdb.COMMAND_USER)
#
#     def invoke(self, arg, from_tty):
#         gdb.execute("layout split")
#
# # Register commands
# Split()
